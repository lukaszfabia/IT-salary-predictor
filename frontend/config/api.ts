export const api: string = `${process.env.NEXT_PUBLIC_API}`;

// for calc
export const endpointKategories: string[] = [
  `${api}/locations/`,
  `${api}/technologies/`,
  `${api}/exp/`,
  `${api}/modes/`,
  `${api}/contracts/`,
];
